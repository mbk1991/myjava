package refactoring.nms.device.enter_window;

import refactoring.nms.device.Device;

public class WindowServer  extends Device {
    public WindowServer(String sysOID) {
        super(sysOID);
    }
}
